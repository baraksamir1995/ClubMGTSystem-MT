# Never commit .env files or editor save-states of them
.env
.env.local
.env.*.local
.env.local.save
*.save
.env.development.local
.env.test.local
.env.production.local

# Dependencies (install locally in each sub-project)
node_modules/
gym-admin/ClubMGTSystem

# Local / OS junk (not for the repo)
.DS_Store
**/.DS_Store
.claude/
SESSION_HANDOFF.md
