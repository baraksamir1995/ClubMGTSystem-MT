import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Splash
  static const Color splashBgTop = Color(0xFF0F172A);
  static const Color splashBgBottom = Color(0xFF1E293B);

  // Onboarding
  static const Color onboardingBg = Color(0xFFF8FAFC);

  // Brand
  static const Color primary = Color(0xFF7C3AED);
  static const Color primaryLight = Color(0xFFEDE9FE);
  static const Color primaryDark = Color(0xFF5B21B6);

  // Text
  static const Color textPrimary = Color(0xFF0F172A);
  static const Color textSecondary = Color(0xFF64748B);
  static const Color textHint = Color(0xFF94A3B8);
  static const Color white = Color(0xFFFFFFFF);

  // Indicator
  static const Color indicatorActive = Color(0xFF7C3AED);
  static const Color indicatorInactive = Color(0xFFE2E8F0);

  // Surface
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFF1F5F9);
  static const Color border = Color(0xFFE2E8F0);
}
